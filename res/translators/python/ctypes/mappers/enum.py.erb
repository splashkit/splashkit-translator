<%#
  Pascal mapper from `enum` <-> `LongInt`
 %>
<%
  @enums.each do |enum|
%>
def __skadapter__to_<%= enum[:name] %>(v):
  return <%= enum[:name].type_case %>(v)

def __skadapter__to_sklib_<%= enum[:name] %>(v):
  return c_int(v.value)

<%
  end # enums.each
%>
