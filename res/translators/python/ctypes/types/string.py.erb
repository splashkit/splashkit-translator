class <%= sklib_prefix() %>_string(Structure):
    _fields_ = [
      ( "str",  c_char_p),
      ( "size", c_int ),
      ( "ptr", c_void_p ),
    ]

    def __init__(self, s):
        self.size = len(s)
        bytes = str.encode(s)
        self.str = c_char_p(bytes)
        self.ptr = None
