<%#
  Declare typealias struct pointer declaration
%>
<%
  @typealiases.each do |typealias|
%>
<%= typealias[:name].type_case %> = ^__sklib_<%= typealias[:name] %>__record_type;
__sklib_<%= typealias[:name] %>__record_type = record end;
<%
  end # end typealias.each
%>
