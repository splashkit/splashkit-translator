<%#
  Mappers from `bool` <-> `i32`
  Based on bindgen's actual implementation
%>
#[inline]
pub(crate) fn __skadapter__to_sklib_bool(v: bool) -> i32 {
    if v { 1 } else { 0 }
}
#[inline]
pub(crate) fn __skadapter__to_bool(v: i32) -> bool {
    v != 0
}