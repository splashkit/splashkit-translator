<%#
  Mappers from `bool` <-> `i32`
%>
fn __skadapter__to_sklib_bool(v: bool) -> i32
{
  if v {
    return -1;
  } else {
    return 0;
  }
}

fn __skadapter__to_bool(v: i32) -> bool
{
  return v != 0;
}
