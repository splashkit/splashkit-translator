<%#
    Define every typealias as a pointer to an internal struct name
 %>
<%
  @typealiases.pluck(:name).each do |typealias|
    internal_data_name = "struct _#{typealias}_data"
%>
<%= internal_data_name %>;
typedef <%= internal_data_name %> *<%= typealias %>;
<%
  end # end typealiases.each
%>
