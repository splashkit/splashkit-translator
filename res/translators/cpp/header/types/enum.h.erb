<%#
    Redeclare all enum types
 %>
<%
  @enums.each do |enum|
    enum_consts = enum[:constants].keys.map(&:to_s).join(",\n")
%>
typedef enum {
    <%= enum_consts.indent %>

} <%= enum[:name] %>;
<%
  end # end enums.each
%>
