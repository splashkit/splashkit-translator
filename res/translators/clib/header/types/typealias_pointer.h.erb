<%#
    `__sklib_ptr' is a wrapper for typealiases to pointer types using static
    casts.
 %>
#define __sklib_ptr void *
<%
  @typealiases.pluck(:name).each do |typealias|
%>
typedef __sklib_ptr __sklib_<%= typealias %>;
<%
  end # end typealiases.each
%>
