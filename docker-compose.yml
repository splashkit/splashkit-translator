---
version: "3"
services:
  headerdoc:
    container_name: headerdoc
    build: .
    stdin_open: true
    tty: true
    entrypoint: ./translate_wrapper
    volumes:
      - ../splashkit-core:/splashkit
